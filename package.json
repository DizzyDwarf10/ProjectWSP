
{
  "name": "projectwsp-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
