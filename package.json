{
  "name": "projectwsp-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cd fitness-tracker && npm run dev",
    "build": "cd fitness-tracker && npm run build",
    "preview": "cd fitness-tracker && npm run preview"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
